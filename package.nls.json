{
  "displayName": "JSONL Viewer",
  "description": "A fast, read-only tree viewer for JSON, JSONL, and NDJSON files.",
  "editor.displayName": "json viewer",
  "configuration.fontSize.description": "Font size used by the JSON tree viewer. Set to 0 to follow the active editor."
}
