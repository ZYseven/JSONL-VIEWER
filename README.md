# JSONL Viewer

JSONL Viewer is a read-only VS Code tree viewer for JSON, JSONL, and NDJSON files.

## Development

```sh
npm install
npm run compile
```

Press `F5` in VS Code to open an Extension Development Host.
